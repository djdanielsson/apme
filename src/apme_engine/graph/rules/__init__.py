"""Graph analysis rules for APME (ADR-059).

Rule files are loaded dynamically by ``apme_engine.graph.scanner.load_graph_rules``
via ``apme_engine.graph._loader.load_classes_in_dir``.  Each rule file defines a
subclass of ``apme_engine.graph.rule_base.GraphRule``.
"""
